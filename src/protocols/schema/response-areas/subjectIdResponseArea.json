{
  "description":"A response area to record a subject id in the exam results",
  "allOf": [
    {"$ref": "/response-areas/definitions/commonResponseAreaProperties.json"},
    {
      "properties": {
        "type": {
          "enum": ["subjectIdResponseArea"]
        },
        "generate": {
          "type": "boolean",
          "description": "If true, the subject id will be generated.",
          "default": false
        },
        "skip": {
          "type": "boolean",
          "description": "DEPRECATED - please use `responseRequired` from commonResponseAreaProperties.json."
        }
      }
    }
  ]
}
