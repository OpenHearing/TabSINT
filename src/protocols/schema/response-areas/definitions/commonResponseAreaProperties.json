{
  "description": "Response areas - common properties supported across all response areas",
  "type": "object",
  "properties": {
    "responseRequired": {
      "description": "Require response to be defined before activating submit button on a page.  Setting to false allows empty submission",
      "type": "boolean",
      "default": true
    },
    "enableSkip": {
      "description": "Make it possible to skip the page.  Overrides any page logic, makes the page submittable, and submits.",
      "type": "boolean",
      "default": false
    }
  }
}
