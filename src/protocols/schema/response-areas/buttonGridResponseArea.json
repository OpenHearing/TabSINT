{
  "description": "Allows choices between an arbitrary number of radio-button choices in a grid.",
  "allOf": [
    {"$ref": "/response-areas/definitions/commonResponseAreaProperties.json"},
    {
      "properties": {
        "type": {
          "enum": [ "buttonGridResponseArea" ]
        },
        "feedback": {
          "type": "string",
          "enum": [
            "gradeResponse",
            "showCorrect"
          ]
        },
        "rows": {
          "type": "array",
          "items": {
            "properties": {
              "choices": {
                "type": "array",
                "items": { "$ref": "/response-areas/definitions/choice.json" }
              }
            }
          }
        },
        "verticalSpacing": {
          "type": "integer",
          "description": "Vertical spacing between buttons, given in [px]"
        },
        "horizontalSpacing": {
          "type": "integer",
          "description": "Horizontal spacing between buttons, given in [px] px"
        },
        "delayEnable": {
          "type": "integer",
          "description": "Delay (ms) before the buttons are active to accept a response",
          "default": 0
        }
      }
    }
  ]
}
