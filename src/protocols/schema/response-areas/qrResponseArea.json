{
  "description": "QR Code input area.",
  "allOf": [
    {"$ref": "/response-areas/definitions/commonResponseAreaProperties.json"},
    {
      "properties": {
        "type": {
          "enum": [
            "qrResponseArea"
          ]
        },
        "scope": {
          "type": "string",
          "enum": ["exam", "page"],
          "description": "If scope is 'exam', then setting this QR code sets the single exam-wide QR code. If 'page', the the code is only recorded as an aswer for this page.",
          "default": "exam"
        },
        "autoSubmit": {
          "type": "boolean",
          "description": "Automatically submit the page when the QR code is scanned",
          "default": true
        },
        "required": {
          "type": "boolean",
          "description": "DEPRECATED - please use `responseRequired` from commonResponseAreaProperties.json."
        }
      }
    }
  ]
}
