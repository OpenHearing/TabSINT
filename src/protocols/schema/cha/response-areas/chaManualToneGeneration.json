{
  "description": "CHA - Manual Tone Generation.",
  "properties": {
    "type": {
      "enum": [ "chaManualToneGeneration"]
    },
    "examInstructions": {
      "type": "string",
      "description": "Replaces the top-level instruction text on the CHA exam pages (each page after starting page)"
    },
    "minLevel":{
      "type": "number",
      "Description": "Minimum level the user can select for playing tones.  Hardware limited."
    },
    "maxLevel":{
      "type": "number",
      "Description": "Maximum level the user can select for playing tones.  Hardware limited."
    },
    "presentationList": {
      "type": "array",
      "description": "Array of frequencies to make available for Tone Generation",
      "items": {
        "type": "object",
        "allOf": [
          { "$ref":"/cha/definitions/toneGenerationLongLevelProperties.json"},
          {
            "properties": {
              "F": {
                "type": "integer",
                "description": "Frequency of tone/center frequency of noise",
                "maximum": 32000,
                "minimum": 1
              }
            }
          }
        ]
      }
    },
    "commonPresentationProperties": { "$ref":"/cha/definitions/toneGenerationLongLevelProperties.json" }
  }
}
