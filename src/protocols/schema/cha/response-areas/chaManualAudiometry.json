{
  "description": "CHA - Manual Audiometry.",
  "properties": {
    "type": {
      "enum": [ "chaManualAudiometry"]
    },
    "examInstructions": {
      "type": "string",
      "description": "Replaces the top-level instruction text on the CHA exam pages (each page after starting page)"
    },
    "boneConduction": {
      "type": "boolean",
      "description": "If true, enable output to bone conductor",
      "default": false
    },
    "showPresentedTones": {
      "type": "boolean",
      "description": "If true, show the tones presented at each frequency on a seperate chart.",
      "default": false
    },
    "audiometryType":{
      "type": "string",
      "enum": ["HughsonWestlake"],
      "description": "Type of audiometry exam to run",
      "default": "HughsonWestlake"
    },
    "minLevel":{
      "type": "number",
      "description": "Minimum level the user can select for playing tones.  Hardware limited.",
      "default": -80
    },
    "maxLevel":{
      "type": "number",
      "description": "Maximum level the user can select for playing tones.  Hardware limited.",
      "default": 100
    },
    "masking": {
      "type": "boolean",
      "description": "If true, enable masking.",
      "default": false
    },
    "minMaskingLevel":{
      "type": "number",
      "description": "Minimum level the user can select for masking noise.  Hardware limited.",
      "default": -80
    },
    "maxMaskingLevel":{
      "type": "number",
      "description": "Maximum level the user can select for masking noise.  Hardware limited.",
      "default": 80
    },
    "onlySubmitFrequenciesTested":{
      "type": "boolean",
      "Description": "If true, only generate results for frequencies tested.  Default: false, submit all",
      "default": false
    },
    "responseType":{
      "type": "string",
      "enum": ["threshold", "pass-fail"],
      "description": "threshold: allows user to set thresholds for each frequency, pass-fail: offers a toggle and sets 'P' or 'F' for each frequency"
    },
    "exportToCSV": {
      "type": "boolean",
      "description": "Whether result should be exported to CSV upon submitting exam results.",
      "default": false
    },
    "presentationList": {
      "type": "array",
      "description": "Array of frequencies to run in manual audiometry. Does not support DynamicStartLevel.",
      "items": {
        "type": "object",
        "allOf": [
          {"$ref": "/cha/definitions/audiometryLevelProperties.json"},
          {
            "properties": {
              "id": {
                "type": "string",
                "description": "Custom presentationId to use for each frequency."
              }
            }
          }
        ]
      }
    },
    "examProperties": { "$ref":"/cha/definitions/hughsonWestlakeExamProperties.json" }
  }
}
