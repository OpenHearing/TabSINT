{
  "description": "Bekesy-Like Audiometry Exam Properties",
  "allOf": [
    { "$ref":"/cha/definitions/audiometryLevelProperties.json"},
    {
      "properties": {
        "ReversalDiscard": {
          "type": "number",
          "description": "Reversals to discard",
          "default": 2,
          "maximum": 10,
          "minimum": 0
        },
        "ReversalKeep": {
          "type": "number",
          "description": "Reversals to keep, must be even",
          "default": 6,
          "maximum": 10,
          "minimum": 2,
          "multipleOf": 2
        },
        "IncrementStart": {
          "type": "number",
          "description": "Increment between presentations until first Reversal, in dB",
          "default": 4,
          "maximum": 20,
          "minimum": 1
        },
        "IncrementNominal": {
          "type": "number",
          "description": "Increment after first reversal, in dB",
          "default": 2,
          "maximum": 20,
          "minimum": 1
        }
      }
    }
  ]
}