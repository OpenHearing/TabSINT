{
  "description": "CHA - common properties for masking noise generation",
  "properties": {
    "Type": {
      "type": "string",
      "description": "Base shape of noise spectrum.",
      "enum": ["white", "pink", "brown"],
      "default": "white"
    },
    "BandpassCenterFrequency": {
      "type": "number",
      "description": "Center frequency for the noise’s bandpass filter; if zero no filtering is applied.",
      "default": 0
    },
    "BandpassOctaveWidth": {
      "type": "number",
      "description": "Width of the pass-band in ocataves.  Range 1/24 - 6",
      "default": 1,
      "maximum": 6,
      "minimum": 0.04166
    },
    "Ear": {
      "type": "integer",
      "description": "Channel to be used for the noise 0=Left, 1=Right, 2=Both",
      "enum": [0, 1, 2],
      "default": 2
    },
    "Level": {
      "type": "array",
      "description": "Level in dB SPL of the noise [Left_Ear Right_Ear]; ignored for non-specified Ear.  dB SPL",
      "items": {
        "type": "integer",
        "default": 30
      },
      "minItems": 2,
      "maxItems": 2
    }
  }
}
