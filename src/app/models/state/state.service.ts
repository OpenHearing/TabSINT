import { Injectable } from '@angular/core';
import { StateModel } from './state.interface';

@Injectable({
    providedIn: 'root',
})

export class StateM {

    stateM: StateModel = {
        name: "name"
    }

}